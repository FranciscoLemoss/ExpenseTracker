import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listItem: {
    backgroundColor: '#fff',
    marginVertical: 8,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
});
